server {
    
   listen 80;

   resolver 127.0.0.11 valid=5s;

   set $app http://app;
   set $auth http://auth/authentication/;

   location / {
       proxy_pass $app;
   }

   location /authentication {
       proxy_pass $auth;
   }

    
}
